extends ../layout 

//- 프로젝트 수정
block content
  .container 
    form(action="/management/project/search", method="post")
      h1.text-center-mt-5 기간 별 프로젝트 정보

      .form-row
        .form-group.col-md-6
          h4 시작 일자
          input.form-control(type="date", name="search_start")
        .form-group.col-md-6
          h4 종료 일자
          input.form-control(type="date", name="search_end")
      .row.justify-content-start.mt-4
            .col(style="margin-bottom: 1em")
            button.btn.btn-outline-dark(type="submit", style="float: right") Search

    if(projects)  
      each project in projects
        if(project.project_emp[0])
          .card-group
            .card 
              h5.project_name= project.project_name
              p.start_date= project.start_date
              p.end_date= project.end_date
              p.duty= project.duty
              p.state= project.state
              .d-grid.gap-2.mt-2
                a.btn.btn-outline-warning(href=`/projects/${project.project_no}`) 자세히
        else 
          .card-group
            .card 
              h5 존재하는 프로젝트가 없습니다.
    else 
      .card-group
          .card 
            h5 검색된 프로젝트가 없습니다.