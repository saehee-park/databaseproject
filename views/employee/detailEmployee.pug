extends ../layout

//- 직원 조회 페이지
block content
  .container
    form(action="/detailEmployee", method="post")
    h1.text-center.mt-5 직원 Information

    div.card-group 
        .card
            .card-body 
                h4 이름
                p.name 박지원
                h4 사번
                p.num 60606060
                h4 부서
                p.dept 개발
                h4 skill
                p.skill html/js
                
                if(authorization == 1)
                    a.btn.btn-outline-dark(href=`/management/project/${project.project_no}/delete`) 직원 삭제</a>
                <a href="#" class="btn btn-outline-dark" style="float: right">List로 돌아가기</a>